#!/bin/bash

# .bootstrap/setup.yml hash: {{ include "private_dot_bootstrap/private_setup.yaml" | sha256sum }}

if command -v ansible-playbook &> /dev/null; then
    ansible-playbook {{ joinPath .chezmoi.sourceDir "private_dot_bootstrap/private_setup.yaml" | quote }}
fi
